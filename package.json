{
  "dependencies": {
    "aplayer": "^1.10.1",
    "atomic-algolia": "^0.3.19"
  },
  "scripts": {
    "build": "hugo --gc && atomic-algolia &&npm run algolia",
    "algolia": "atomic-algolia"
  }
}
